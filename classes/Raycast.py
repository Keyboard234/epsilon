class Raycast:
    def __init__(self, position, angle, maxrange):
        self.startpos = position

        self.angle = angle
        self.maxrange = maxrange
    def gethit(self):
        pass


        """
        if hittype == "enemy":
            return "enemy", dist, pos
        else:
            return "map", dist, pos #TODO: add material
        """
